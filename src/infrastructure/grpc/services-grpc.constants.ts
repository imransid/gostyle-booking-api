/** Injection token for the raw gRPC client. Not the same as
 *  SERVICES_DIRECTORY: that one names the PORT, this one names the
 *  transport client the adapter wraps. */
export const SERVICES_DIRECTORY_CLIENT = 'SERVICES_DIRECTORY_CLIENT';
