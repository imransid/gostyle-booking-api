/** Injection token for the raw gRPC client. Not the same as
 *  PRODUCTS_DIRECTORY: that one names the PORT, this one names the
 *  transport client the adapter wraps. */
export const PRODUCTS_DIRECTORY_CLIENT = 'PRODUCTS_DIRECTORY_CLIENT';
